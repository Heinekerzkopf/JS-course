'use strict'

//========================================================================================================================================================

// basic Number methods

// converting string into number:

// 1.
Number('52');

// 2.

+'52';

//========================================================================================================================================================

// 3. string+number

console.log(Number.parseInt('12px')); // but parseInt doesnt take numbers after .95120; writing 12.5 = will also be 12

// 4. 

console.log(Number.parseFloat('12.50912')); // thats why we can use parseFloat - if we want numbers after .124124; but if we dont want them - we use parseInt;

//========================================================================================================================================================

// 5. max and min

console.log(Math.max(51251, 235,123,4647, 643,12));
console.log(Math.min(51251, 235,123,4647, 643,12));

//========================================================================================================================================================

// 6. Math.trunc - returning whole number

// 7. Math.random , Math.floor

function calcRandomNumb(min, max) {
    return Math.floor(Math.random() * (max - min) + 1) + min
}

console.log(calcRandomNumb(10, 25))

//========================================================================================================================================================

// 8. Math.round - number rounding

console.log(Math.round(12.95325));
console.log(Math.round(12.4));

// 9. Math.ceil - round to upper number
// 10. Math.floor - round to lower number
// 11. .toFixed(2) - helps to leave fixed numbers after point

//========================================================================================================================================================
//========================================================================================================================================================
//========================================================================================================================================================

// bigInt - data type

// max JS number :

console.log(2 ** 53 - 1);

//========================================================================================================================================================

console.log(typeof 3195807359315801385103580139n); // using bigInt, putting 'n' and the end

//========================================================================================================================================================

// bigInt doesnt work with Math.  methods

console.log(490125812905812509215n + 590328509235n);

//========================================================================================================================================================
//========================================================================================================================================================
//========================================================================================================================================================

// how to make date

const date = new Date(2025, 4, 10, 17, 23, 50);

console.log(date);

date.setFullYear(2035)

// new Date - has also some methods, lets check

console.log(date.getFullYear());
console.log(date.getMonth());
console.log(date.getDate());
console.log(date.getDay());
console.log(date.getMinutes());
console.log(date.getSeconds());
console.log(date.getMilliseconds());
console.log(date.getHours());
console.log(date.getTime());

//========================================================================================================================================================

console.log(date.toISOString());

//========================================================================================================================================================

const future = new Date(2025, 3, 15);
const now = new Date(2025, 2, 10, 17, 30);
const result = +future - +now;

console.log(Math.round(result / 1000 / 60 / 60 / 24));

//========================================================================================================================================================
//========================================================================================================================================================
//========================================================================================================================================================

const local = navigator.language;

console.log(local);

const options = {
    year: 'numeric',
    // hour: "numeric",
    // minute: 'numeric',
    day: 'numeric',
    // weekday: 'long',
    month: 'numeric', // 'short'
    // second: 'numeric',
    // timeZoneName: 'long',
}

const now2 = new Date();

const gb = Intl.DateTimeFormat(local, options).format(now2);

console.log(gb);

//========================================================================================================================================================
//========================================================================================================================================================

const num = 53050939053;

const localNumberFormat = navigator.language;

const numberOptions = {
    style: 'currency',
    currency: 'RUB',
}

const ru = Intl.NumberFormat(localNumberFormat, numberOptions).format(num);
const ge = Intl.NumberFormat('de-DE').format(num);
const arab = Intl.NumberFormat('ar-SY').format(num);

console.log(ru);
console.log(ge);
console.log(arab);

//========================================================================================================================================================
//========================================================================================================================================================
// setInterval, setTImer 

setTimeout(function () {
    console.log('hello wrold');
}, 2000);

//========================================================================================================================================================

const timer1 = setTimeout(function (word1, word2) {
    console.log(`${word1} ${word2}`);
}, 2000, 'hello', 'wrold');

const timer2 = setInterval(() => {
    console.log('hi');
}, 2000);

if (true) {
    clearTimeout(timer1)
    clearInterval(timer2)
}

//========================================================================================================================================================
//========================================================================================================================================================
//========================================================================================================================================================
